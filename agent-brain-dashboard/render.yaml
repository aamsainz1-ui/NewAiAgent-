services:
  - type: web
    name: openclaw-brain-api
    env: node
    buildCommand: npm install
    startCommand: node server.cjs
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
